services:
  backend:
    build: ./backend
    image: backendgame # <--- You define the name here
    env_file:
      - ./backend/.env
    ports:
      - "4000:4000"

  frontend:
    build: ./frontend
    image: frontendgame
    ports:
      - "5173:80"
    depends_on:
      - backend
